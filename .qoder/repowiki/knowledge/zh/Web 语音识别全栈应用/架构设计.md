- **前后端分离与代理模式**：`server.js` (Express) 或 `api/token.php` 作为安全边界，持有阿里云 AccessKey 并调用 POP API 获取临时 Token，避免密钥泄露至前端。
- **多后端策略路由**：前端 `speech.js` 封装统一识别接口，根据配置动态切换浏览器原生 Web Speech API 或阿里云 WebSocket API，并在原生 API 网络失败时自动降级/切换至阿里云引擎。
- **静态资源托管**：后端服务同时承担 `index.html`、CSS 及 JS 模块的静态文件托管职责，实现单端口部署。